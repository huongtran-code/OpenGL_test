{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run OpenGL (Terminal Only)",
            "type": "cppvsdbg", // hoặc cppdbg tùy máy bạn
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": true, // Mở một cửa sổ CMD riêng biệt để log terminal
            "preLaunchTask": "build OpenGL"
        }
    ]
}